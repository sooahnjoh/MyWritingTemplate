# 작업 규칙

- 파일을 새로 만들어야 할 것 같으면 만들지 말고 **나에게 먼저 물어본다**
- 지금 시킨 것과 **관계없는 코드를 고치지 않는다**
- 내가 **시키지 않은 기능**을 미리 만들지 않는다
- 기술 스택은 `specs/tech-stack.md` 를 따른다. **거기 없는 라이브러리를 설치하지 않는다**
- 티켓 하나가 끝나기 전에 **다음 티켓을 시작하지 않는다**
- 다 만든 뒤, **바꾼 것을 파일 이름과 함께 3줄로** 요약한다
- 다 만든 뒤, **내가 손가락으로 확인할 절차**를 Given / When / Then 으로 알려준다
- 확실하지 않으면 추측하지 말고 **나에게 물어본다**

## 내가 추가한 줄
테스팅
-
